//c program to print the size of int
#include<stdio.h>
void main()
{
    printf("The size of integer is %d\n",sizeof(int));//%d is the format specifer of int and sizeof(datatype) keyword is used to return the size of the datatype
}
